name: Broken link checker
on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * *'
  

jobs:
  linkinator:
    runs-on: ubuntu-latest
    steps:
      - uses: JustinBeckwith/linkinator-action@v1
        with:
          paths: 